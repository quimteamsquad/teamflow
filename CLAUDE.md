# TeamFlow

TeamFlow is a spec-driven development plugin. Every feature follows:
spec -> plan -> tasks -> work -> review -> production.

## Core Rules
- NEVER write code without an approved spec (except /quick for fixes).
- NEVER skip code review.
- All state lives in `.workflow/` as Markdown and YAML files.

To get oriented, use the skill teamflow:using-teamflow.
